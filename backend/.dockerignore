# Secrets — MUST never enter the image.
.env
.env.*
*.key
secrets/

# Tests + CI aren't needed at runtime (smaller image, less surface).
tests/
.github/
calibration_harness.py

# Python / tooling cruft
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# VCS / editor / OS
.git/
.gitignore
.idea/
.vscode/
.DS_Store
